AGENT: duplication
FINDINGS: none
SUMMARY: No significant duplication detected across implementation files. This is a pure value-substitution change (maxNoteTextLen 500->2000 plus three test boundary values 500/501->2000/2001); the git diff confirms no logic blocks were introduced or restructured. The repeated find-task-by-ID loops and setup helpers visible in internal/cli/note_test.go are pre-existing code untouched by this change and therefore out of scope. The one incidental repetition — the 2000 boundary literal appearing in notes.go (const), notes_test.go, and note_test.go — is a single magic-value drift point rather than duplicated logic: it is below the extraction threshold (Rule of Three applies to blocks, not lone literals), note_test.go in package cli cannot reference the unexported const anyway, and explicit boundary literals in tests are a legitimate, intentional convention. No consolidation warranted.
