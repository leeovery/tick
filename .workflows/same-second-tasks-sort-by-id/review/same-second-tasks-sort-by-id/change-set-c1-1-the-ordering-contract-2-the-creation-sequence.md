SECTION: 1. The Ordering Contract / 2. The Creation Sequence

SCOPE: 3b58fe05ac5bd41a6f0632ea63570dbd3db53b40..HEAD (first task commit 139ebe18 through cebf3c74), filtered to the 28 listed files. Read: `git log` and `git diff` over the range for internal/task/task.go, internal/storage/{jsonl,store,cache}.go, internal/cli/{create,list,show}.go, internal/migrate/store_creator.go and README.md, then the files in their final state. Followed behaviour outside the list into internal/cli/app.go (ready/blocked/list dispatch), internal/cli/{remove,update,note,dep,transition,migrate}.go (every Mutate caller), internal/cli/json_formatter.go (detail-document marshalling) and internal/cli/show_fields.go (field registry). Read the section's tests: internal/cli/{list_order,backfill_seq,create_seq,mutate_seq,migrate_seq}_test.go, plus the new cases in internal/storage/{jsonl,cache}_test.go, internal/task/task_test.go, internal/migrate/store_creator_test.go and internal/cli/migrate_test.go. Ran: `gofmt -l ./internal ./cmd`, `go vet ./...`, `golangci-lint run ./...`, `go build -o <scratchpad>/tick ./cmd/tick/`, one targeted `go test` run for the unsettled regression-floor criterion, and two end-to-end runs of the built binary in throwaway scratchpad projects. All scratch artefacts were removed afterwards. The only tree changes are the pre-existing manifest.json modification and the review/ directory.

MEASURED:
- "The existing priority-then-created ordering tests (`ready_test.go:306`, `blocked_test.go:212`, `list_filter_test.go:368`) pass unmodified (§8.5)" [1-1] — `git diff --stat 3b58fe05..HEAD -- internal/cli/ready_test.go internal/cli/blocked_test.go internal/cli/list_filter_test.go` → empty (unmodified). `go test ./internal/cli -run 'TestReady|TestBlocked|TestListFilter' -count=1` → `ok github.com/leeovery/tick/internal/cli 6.755s`. The named subtests sit in TestReady, TestBlocked and TestListFilter. — holds
- "The existing priority-then-created ordering tests (`ready_test.go:306`, `blocked_test.go:212`, `list_filter_test.go:368`) pass unmodified" [1-2] — same measurement as [1-1]: empty diff over the range, and `go test ./internal/cli -run 'TestReady|TestBlocked|TestListFilter' -count=1` → ok — holds

NOT MEASURED:
- "The rest of the suite passes unchanged, including the create and migrate `Seq: task.NextSeq(tasks)` assignments and the migrate `mockStore` subtests (§8.5)" [1-6] — settling this needs a whole-suite `go test ./...`, which this verifier may not run; the suite runs once later over the corrected tree. Reading confirms that both assignments are present and unchanged at internal/cli/create.go:221 and internal/migrate/store_creator.go:82.
- "The conformance inventory's entries, including those for `create`, `update`, `note add` and `note remove`, still decode under both the TOON and JSON drivers. The existing conformance and README-sample tests pass without any fixture or sample changing" [2-3] — the subject belongs to §4, Query Changes (§4.4, what the sub-list change reaches), not to this section.
- "The README sample run passes with no sample, fixture or exemption changed: both sentences sit outside every fence (§6)" [3-3] — the subject belongs to §6, Documentation, not to this section.

FINDINGS:
- None

COVERAGE:
- Both list-family clauses end on the sequence and then the task ID, with the ready band first and priority, then created, above the sequence — internal/cli/list.go:313, :315 — read
- `LIMIT ?` is appended after the now-total ORDER BY, so `--count` cuts the authored order — internal/cli/list.go:318-321 — read; measured with the built binary: a shell loop creating `step-1`…`step-5` under one parent in one second (IDs non-ascending) → `tick ready --parent P --count 1` returned step-1, and `tick list --parent P` and `tick ready` returned step-1…step-5
- list, ready and blocked all run through RunList, and so through buildListQuery; no other list-family ORDER BY exists in production code — internal/cli/app.go:192, :223, :236; `rg -n 'ORDER BY' internal --glob '!*_test.go'` — read
- The order is total: the final term `t.id` is the tasks primary key, so no two rows can tie on the whole key — internal/storage/cache.go:18-30, internal/cli/list.go:313-315 — read
- The timestamp format is unchanged and all nine truncation sites remain — internal/task/task.go:41 — measured: `sed -n 41p internal/task/task.go` → `const TimestampFormat = "2006-01-02T15:04:05Z"`; `rg -c 'Truncate\(time\.Second\)' internal/ --glob '!*_test.go'` → 9 in total
- The `seq` field round-trips through the stored record (MarshalJSON writes it, UnmarshalJSON reads it, omitempty drops only zero) — internal/task/task.go:57, :77, :98, :136 — read; pinned by the new round-trip subtest in internal/storage/jsonl_test.go
- A new task takes one above the highest sequence in the as-read (already backfilled) task set, and starts at 1 when none exists — internal/task/task.go:274-280, internal/cli/create.go:221, internal/migrate/store_creator.go:82 — read; pinned by TestNextSeq, TestCreateAssignsSequence ("one above the highest, not the last line or the count" → 8) and the store_creator_test.go subtests
- Assignment is race-free: Mutate takes the exclusive lock (store.go:176), reads and backfills inside it (:182 → readAndEnsureFresh → ParseJSONL :365), then calls fn (:188) — internal/storage/store.go:175-192 — read
- Backfill numbers above the file's highest sequence, not by a running maximum, treats zero the same as absent, and never renumbers a carried sequence — internal/storage/jsonl.go:116, :123-131 — read; measured with the built binary on the merge shape a1 b2 c3 x- y- d4 e5 in one second: `tick list` → a b c d e x y; after `tick update x`, the stored seqs were 1 2 3 6 7 4 5 and `tick doctor` exited 0 with "Sequence uniqueness: OK"
- ParseJSONL is the only production parse path, reached by ReadTasks, Rebuild and readAndEnsureFresh — internal/storage/store.go:164, :248, :365; `rg -n 'ParseJSONL|ReadJSONL' internal cmd --glob '!*_test.go'` — read; pinned by the backfill_seq_test.go subtest "it assigns the same sequences through ReadTasks, rebuild and list"
- Mutate numbers any Seq-0 task that fn returns, using the same backfillSeqs rule, so every write persists a sequence on every record. This goes beyond the written spec (§2.2 names only creation) and is sound: the rule is shared, and a carried number is never changed — internal/storage/store.go:192 — read; pinned by TestMutateNumbersUnnumberedTasks, whose seqs are asserted raw as [1 2 3 6 7 4 5]
- Carried sequences survive update, remove, create and rebuild. Removing the highest frees its number without creating a duplicate — internal/cli/remove.go:184-188 (filters the slice), internal/cli/update.go:275 (edits in place) — read; pinned by the create_seq_test.go subtests "keeps … through update, remove, create and rebuild" and "reuses a removed highest sequence without duplicating one"
- A legacy file with no sequences lists in line order and stays byte-identical on reads. The assignment becomes permanent on the next write — pinned by the backfill_seq_test.go subtests "no sequences in line order through update, remove and create" and "byte-identical on list" — read
- The cache carries the sequence (schema v3, `seq` column, rebuild insert), and a v2 cache is rebuilt with seq populated — internal/storage/cache.go:15, :29, :139, :208 — read; pinned by TestSchemaV2CacheUpgrade and the cache_test.go subtest "it stores each task's creation sequence in the seq column"
- Imports: a source with no creation times keeps import order, and one with real creation times is ordered by those times to whole-second resolution — internal/migrate/store_creator.go:60-63, :82 — read; pinned by TestMigrateAssignsSequence (four subtests, including "across seconds" and "whatever the source fractions")
- The sequence appears in no command output, detail document, field registry, help text or README sample. The JSON formatter marshals its own structs rather than task.Task, and no production code dumps structs with %+v or %#v — internal/cli/json_formatter.go:403-411, internal/cli/show_fields.go — read: `rg -n -i '\bseq\b|sequence' README.md internal/cli/show_fields.go internal/cli/help.go` → only the §6 doctor line; measured with the built binary: `tick --json show P | grep -c '"seq"'` → 0. Pinned by TestSequenceNotSurfaced (toon, JSON, pretty, and `--field seq` refused)
- Lint and build are clean — whole module — run: `gofmt -l ./internal ./cmd` → no output; `go vet ./...` → rc 0; `golangci-lint run ./...` → "0 issues."; `go build -o <scratchpad>/tick ./cmd/tick/` → rc 0 (artefact removed)
