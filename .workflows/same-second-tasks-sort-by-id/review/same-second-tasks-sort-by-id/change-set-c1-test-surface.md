SECTION: test surface

SCOPE: 3b58fe05ac5bd41a6f0632ea63570dbd3db53b40..HEAD (139ebe18 through cebf3c74), limited to the 28 listed files. I read every test file in the change-set in its final state: internal/cli/{list_order,backfill_seq,create_seq,mutate_seq,migrate_seq,show_order,show_blockers_order,mutation_sublist_order}_test.go, internal/doctor/duplicate_seq_test.go, and the diffs to internal/cli/{doctor,migrate,readme_samples}_test.go, internal/storage/{cache,jsonl}_test.go, internal/task/task_test.go and internal/migrate/store_creator_test.go. I held them against spec §8 (every bullet), §2.2–2.4, §3.1–3.4, §4.1–4.4, §5.1–5.2 and §6. I also read the production diff they guard (list.go, show.go, cache.go, jsonl.go, store.go, task.go, create.go, store_creator.go, duplicate_seq.go, doctor.go, README.md) and the helpers the tests call (setupTickProject, readPersistedTasks, runToonCommand, runShow, readmeSection, assertReadOnly, getJSONLines, outputMutationResult, collectScopedStoredEdges). Commands run: `gofmt -l ./internal ./cmd` found no files. `golangci-lint run ./...` reported 0 issues. Targeted runs of the single package `go test ./internal/cli -run … -count=1` settled the unsettled criteria. Mutation probes checked whether the guards hold: I copied the tree (no .git) into the session temp directory, applied one mutation at a time, ran `go test <pkg> -run <tests> -count=1`, and removed the copy afterwards. The tracked tree was never modified, and `git status` matches the session start.

MEASURED:
- "The existing priority-then-created ordering tests (`ready_test.go:306`, `blocked_test.go:212`, `list_filter_test.go:368`) pass unmodified (§8.5)" [1-1] — `git diff --stat 3b58fe05..HEAD -- internal/cli/ready_test.go internal/cli/blocked_test.go internal/cli/list_filter_test.go` is empty. `go test ./internal/cli -run 'TestReady|TestBlocked|TestListFilter' -count=1 -v` → ok. TestReady/it_orders_by_priority_ASC_then_created_ASC, TestBlocked/it_orders_by_priority_ASC_then_created_ASC and TestListFilter/it_maintains_deterministic_ordering each PASS — holds
- "The existing priority-then-created ordering tests (`ready_test.go:306`, `blocked_test.go:212`, `list_filter_test.go:368`) pass unmodified" [1-2] — same measurement as [1-1]: the diff is empty and `go test ./internal/cli -run 'TestReady|TestBlocked|TestListFilter' -count=1` → ok — holds
- "The conformance inventory's entries, including those for `create`, `update`, `note add` and `note remove`, still decode under both the TOON and JSON drivers. The existing conformance and README-sample tests pass without any fixture or sample changing" [2-3] — conformance_test.go is absent from the change-set diff. The pattern the verifier named, `TestConformance`, misses the decoding tests (TestToonOutputConformance, TestJSONOutputConformance, TestToonMutationConformance, …), so I ran `go test ./internal/cli -run 'Conformance|TestFieldSelectionExemptions' -count=1 -v` → all 20 top-level tests PASS, TestToonOutputConformance, TestJSONOutputConformance and TestToonMutationConformance among them. `go test ./internal/cli -run 'TestConformance|TestREADME' -count=1` → ok, with TestREADMESamplesMatchRenderedOutput PASS — holds
- "The README sample run passes with no sample, fixture or exemption changed: both sentences sit outside every fence (§6)" [3-3] — `go test ./internal/cli -run 'TestConformance|TestREADME' -count=1 -v` → TestREADMESamplesMatchRenderedOutput, TestREADMEPromptedSamplesAreClaimed, TestREADMEDocumentsSortContract and TestREADMEDocumentsDuplicateSequenceCheck all PASS over the whole change-set — holds

NOT MEASURED:
- "The rest of the suite passes unchanged, including the create and migrate `Seq: task.NextSeq(tasks)` assignments and the migrate `mockStore` subtests (§8.5)" [1-6] — this needs the whole suite (`go test ./...`), which this pass is not permitted to run. The suite runs once, later, over the corrected tree. By reading: the two NextSeq assignments (internal/cli/create.go:221, internal/migrate/store_creator.go:82) are unchanged since task 1-3/1-5, and the only change to the mockStore subtests is two added subtests (internal/migrate/store_creator_test.go:262, :284).

FINDINGS:
- None

COVERAGE:
- A same-second tie lists in sequence order from unfiltered `list` and from `list --parent`. The fixture IDs descend against authoring order, and a second fixture makes line, ID and sequence order all differ — internal/cli/list_order_test.go:88-123 — measured: with `t.seq ASC` removed from both clauses at internal/cli/list.go:313/315, `go test ./internal/cli -run 'TestTotalListFamilyOrdering|TestSameSecondOrdering|TestBackfillSequence|TestMutateNumbers|TestMigrateAssignsSequence|TestCreateAssignsSequence' -count=1` → red in all four TestSameSecondOrdering subtests plus 17 others
- `ready --parent P --count 1` returns step-1 rather than the lowest ID — internal/cli/list_order_test.go:101-106 (also internal/cli/mutate_seq_test.go:95) — measured: red under the same seq-term removal
- The ready band outranks the sequence: an in_progress task authored after tied open tasks, with the lowest ID, stays first — internal/cli/list_order_test.go:365-381 — measured: with the band term removed from internal/cli/list.go:313, `go test ./internal/cli -run 'TestTotalListFamilyOrdering|TestSameSecondOrdering' -count=1` → red on this subtest
- `tick blocked` carries its own tie assertion — internal/cli/list_order_test.go:383-400 — measured: red under the seq-term removal
- The ID term is the final key under a duplicate sequence for `list`, `list --parent` and `ready`, with line order contradicting ID order — internal/cli/list_order_test.go:402-429 — measured: with `, t.id ASC` removed from internal/cli/list.go:313/315 → red on this subtest
- `created` outranks `seq` in the neutral clause — internal/cli/migrate_seq_test.go:96-110 plus the existing list/blocked/list_filter ordering tests — measured: with `t.seq` swapped above `t.created` at internal/cli/list.go:315, `go test ./internal/cli -run 'TestMigrateAssignsSequence|TestList|TestBlocked' -count=1` → red in TestMigrateAssignsSequence/…across_seconds, TestList, TestBlocked and TestListFilter
- `created` outranks `seq` in the ready clause — internal/cli/ready_test.go:306 (unchanged; ParseJSONL's backfill gives tick-hi1111 seq 4 against tick-hi2222's seq 2, so the fixture now separates the keys) — measured: with the swap at internal/cli/list.go:313, `go test ./internal/cli -run 'TestReady|…' -count=1` → red in TestReady/it_orders_by_priority_ASC_then_created_ASC
- An in-process migration batch with identical timestamps lists in import order, and source fractions are flattened to one second — internal/cli/migrate_seq_test.go:52-77, :112-141 — read: the tests retry until the batch shares a second and the IDs are not ascending. Measured: `go test ./internal/cli -run 'TestCreateAssignsSequence/it_lists_same-second|TestMigrateAssignsSequence' -count=30` → ok, no flake in 30 runs
- Assignment starts at 1, is monotonic, takes one above the highest (not the last or the count), survives update, remove, create and rebuild, and a removed highest number is reused without a duplicate — internal/cli/create_seq_test.go:63-141, internal/task/task_test.go:651 — read: each subtest reads raw on-disk `seq` through rawRecords (internal/cli/backfill_seq_test.go:30), not the read-time backfill
- Create's own `Seq: task.NextSeq(tasks)` (internal/cli/create.go:221) is not observable on its own: Mutate's backfill (internal/storage/store.go:192) assigns the same N+1 — measured: with that line removed, `go test ./internal/cli -run 'TestCreateAssignsSequence|TestBackfillSequence|TestSameSecondOrdering' -count=1` → ok. The behaviour §2.2 requires (a new task takes N+1) is still pinned end to end, so no guard is missing
- The migrate StoreTaskCreator numbers above the highest sequence and starts at 1 — internal/migrate/store_creator_test.go:262-295 — measured: with internal/migrate/store_creator.go:82 removed, `go test ./internal/migrate -run TestStoreTaskCreator -count=1` → red on both subtests (mockStore does not backfill)
- Backfill of a file with no seq follows line order through update, remove and create, and a list read leaves tasks.jsonl byte-identical — internal/cli/backfill_seq_test.go:130-165 — measured: with `backfillSeqs(tasks)` removed from internal/storage/jsonl.go:116, `go test ./internal/cli -run 'TestBackfillSequence|TestSameSecondOrdering' -count=1` → red in 8 of 9 backfill subtests
- Mixed file: the newest unnumbered record orders last, not by line position — internal/cli/backfill_seq_test.go:178-189 — read: seqs 5-7 plus one bare record, so a line-position rule would give it 4 and it would sort first. The test asserts cache seq 8 directly
- Merge shape: backfill numbers above every carried seq, never onto one, on all three read paths (ReadTasks, rebuild, list) and on a mutated slice — internal/cli/backfill_seq_test.go:191-196, :241-265, internal/cli/mutate_seq_test.go:114-138, internal/storage/jsonl_test.go:761 — measured: with backfill swapped for a running maximum in internal/storage/jsonl.go:123, the cli tests go red (merge, ReadTasks/rebuild/list, doctor-after-write, mutate merge) and `go test ./internal/storage -run TestParseJSONLBackfillsSequence -count=1` goes red
- Backfill counts records, not lines, across blank lines, and seq 0 is treated exactly like an absent seq — internal/cli/backfill_seq_test.go:205-239 — read: the cache values 1,2,3 would fail a line-number rule, which gives 1,3,6
- Mutate numbers unnumbered tasks before writing and caching, and the cache agrees with rebuild and ReadTasks — internal/cli/mutate_seq_test.go:78-112 — measured: with internal/storage/store.go:192 removed, `go test ./internal/cli -run TestMutateNumbersUnnumberedTasks -count=1` → red on all 4 subtests
- The cache seq column is asserted directly, not only through order — internal/storage/cache_test.go:214, internal/cli/list_order_test.go:192-243 — measured: inserting 0 for `t.Seq` at internal/storage/cache.go:208 → red in TestSchemaV2CacheUpgrade, TestCreateAssignsSequence and TestBackfillSequence
- A v2 cache rebuilds at v3 with seq and ordinals filled in — internal/cli/list_order_test.go:192, internal/cli/show_blockers_order_test.go:137-164 — measured: with schemaVersion left at 2 in internal/storage/cache.go, both tests go red
- Seq round-trips through JSONL byte-identically — internal/storage/jsonl_test.go:210 — measured: with Seq dropped from MarshalJSON (internal/task/task.go), `go test ./internal/storage -run TestRoundTrip -count=1` → red
- Seq is not surfaced: no seq key in toon or JSON show output, no seq line in pretty output, and `--field seq` is rejected — internal/cli/list_order_test.go:265-321 — read: hasKey walks the decoded document recursively
- Show children follow created, then seq, then id, and priority does not take part. The fixtures' IDs and line order both contradict authoring order — internal/cli/show_order_test.go:49-96 — measured: priority added to internal/cli/show.go:146 → red on the better-priority subtest; seq swapped above created → red on the earlier-second subtest; id term removed → red on the shared-seq subtest
- `--field children.1/.3` and `--field blocked_by.1` pick by the new order — internal/cli/show_order_test.go:98-110, internal/cli/show_blockers_order_test.go:166-170 — read: under ID order children.1 would be tick-aaa111 (c3), not c1
- Blockers follow declaration order, which contradicts both creation and ID order, and match the stored blocked_by array and dep tree's from→to edges — internal/cli/show_blockers_order_test.go:108-135 — read: dep tree edges are From=blocker, To=blocked (internal/cli/dep_tree_graph.go:196). Measured: with ordinal inserted as 0 at internal/storage/cache.go:214, `go test ./internal/cli -run 'TestShowBlockersOrder|TestMutationDetailSubListOrder' -count=1` → red in all 5 show-blocker and all 4 mutation subtests
- The update, note add, note remove and create detail documents carry the same children and blocked_by order as show — internal/cli/mutation_sublist_order_test.go:54-96 — read: every one of these handlers renders through queryShowData (internal/cli/helpers.go:23). Measured red under the ordinal-zero mutation
- The duplicate-seq check reports each group with its line numbers at warning severity, gives a single pass on clean, no-seq, empty and zero-seq files, skips invalid JSON, and is read-only — internal/doctor/duplicate_seq_test.go:37-164 — measured: with the zero skip removed from internal/doctor/duplicate_seq.go, `go test ./internal/doctor -run TestDuplicateSeqCheck -count=1` → red
- `tick doctor` exits 0 on a duplicate, prints the exact warning line with both line numbers, registers the check (11 check marks) and passes after a merge-shaped write — internal/cli/doctor_test.go:849-914 — measured: SeverityError at internal/doctor/duplicate_seq.go:68 → red on the exit-0 subtest; unregistering the check in internal/cli/doctor.go → red in TestDoctorFourChecks, TestDoctorTenChecks and TestDoctorDuplicateSequence
- README prose pins both the sort-contract sentence and the separate "duplicate creation sequences" doctor entry — internal/cli/readme_samples_test.go:691, :725 — measured: removing the tiebreak clause from README.md → TestREADMEDocumentsSortContract red; folding the doctor entry back into "duplicates" → TestREADMEDocumentsDuplicateSequenceCheck red
- The one pre-existing test the work had to change (the migrate "values already in storage untouched" test) still asserts every other field unchanged and the gained seq equal to 1 — internal/cli/migrate_test.go:713 — read
- Test isolation: every fixture is under t.TempDir() (setupTickProject at internal/cli/create_test.go:19, setupTickDir at internal/doctor/cache_staleness_test.go:16), and no test reaches the developer's .tick or home directory — read
- The declared linters pass over the tree — measured: `gofmt -l ./internal ./cmd` → no output; `golangci-lint run ./...` → 0 issues
