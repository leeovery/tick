# List --version in top-level help

`--version` is not listed in `printTopLevelHelp` global flags output — users seeing `tick --help` won't discover it. Worth a follow-up to add it to the help text alongside `--help`.

Source: review of add-version-flag/add-version-flag
