## Attempt 1

ISSUES:
- `internal/cli/show_fields.go:51` + `:263-264` — `showListSections` is a second, hand-maintained declaration of which registry entries are list sections, and its divergence from `showFields` is silent in both directions. Add a sixth list section to the registry with a `noun` and `length` and forget the slice: `addValue` accepts `newsec.99` (`isList()` is true), `ValidatePositions` never sees the name, and `tick show --field newsec.99` returns an empty section and exit 0 instead of §9.6's out-of-range error — verbatim the failure the task's Problem statement names, and no test or compiler catches it. The reverse is worse: a name in the slice with no registry entry makes `field.length(detail)` a nil-func call, panicking every `--field <section>.<n>` invocation. The task's Do prescribed the slice (order cannot come from a map), and the executor flagged the residual honestly — but AC2 demands that adding a section to the registry cannot silently escape range-checking, and as it stands it can.
  FIX: Add a drift-detection test beside `TestValidatePositions` in `internal/cli/show_fields_test.go`, following the package's existing precedent `TestCommandFlagsMatchHelp` (`internal/cli/flag_validation_test.go:318`), which guards exactly this class of coupling. Collect `name` for every `showFields` entry where `field.isList()`, sort it, compare with `slices.Sorted(slices.Values(showListSections))` via `slices.Equal`, and fail naming both sides. Ten lines, keeps the Do's explicit document-order declaration intact, and turns the silent divergence into a red suite.
  ALTERNATIVE: Declare the order on the entry instead — an `order int` on `showField`, with `ValidatePositions` collecting the `isList()` entries and sorting by it — which deletes `showListSections` and literally satisfies "without a second edit". Tradeoff: it fully meets AC2's wording and the task's title, but trades one silent drift for another (a new entry defaulting to `order: 0` sorts first, giving a wrong-but-checked error precedence), and it departs from the Do's explicit instruction to keep document order as one ordered slice. The reviewer recommends the drift test: it preserves the prescribed shape and the failure it leaves — "you must also append to `showListSections`" — is loud rather than silent.
  CONFIDENCE: medium

COMMENT_CORRECTIONS:
- `internal/cli/show_fields.go:49-50` — the rewrite dropped "toon" for "the detail document", and the order is only the toon document's: pretty renders `tags` in the header block (`pretty_formatter.go:156`) ahead of `blocked_by`, and JSON emits `tags, refs, notes … blocked_by, children` (`json_formatter.go:121-138`). A maintainer checking the comment against `tick show --json` would read it as false and could "correct" the slice, silently changing which out-of-range error surfaces first.
  OLD: // showListSections names the list sections in the order the detail document
// renders them, the order an out-of-range error reports the first failure in.
  NEW: // showListSections names the list sections in the order the toon document
// renders them, the order an out-of-range error reports the first failure in.

NOTES:
- `TestTaskDetailWithoutFieldSelection` asserts with `strings.Contains` over eight substrings although all three outputs are deterministic, which code-quality.md lists as an anti-pattern. Not raised as an issue: the test's job is the nil-safety of one gate across three formats, exact full-document output for nil `Fields` is already pinned byte-for-byte by the toon, pretty and JSON suites, and triplicating those goldens here would buy coverage that already exists.
- `internal/cli/show_fields.go:44-45` are 142 columns, past the ~120 the golang-code-style skill states as a MUST-break. Not raised: 369 lines in `internal/cli` exceed 130, and the aligned one-entry-per-line map literal is the clearer form here.
- `toon_formatter.go:99` and `json_formatter.go:139` still gate on `"changed"`, a name `showFields` does not carry — untouched by this task and owned by pending task 4-12.
- The new `TestTaskDetailWithoutFieldSelection` genuinely exercises the surviving gate: with `detail.Fields` nil every formatter reaches `includes` on a nil receiver, and without the `s == nil` guard the very next term dereferences `s.whole`.
