AGENT: standards
FINDINGS:
- FINDING: §12.2's amendments to the two completed specifications have not been made
  SEVERITY: high
  FAILURE: The knowledge base keeps serving the two claims this work replaces as validated context. `tick-core` still states "Long text fields get their own unstructured sections" as a numbered principle and prints the indented raw description block as its worked example; `auto-cascade-parent-status` still fixes the arrow-and-`(auto)` lines as "Flat lines … for machine parsing" and still describes a `FormatTransition` method that no longer exists on the Formatter interface. The next agent designing tick output queries the knowledge base, is handed the superseded shape at full confidence, and rebuilds the defect §6.2 and §7.2 removed. Noticed only when that output ships malformed again — which is exactly the history §11 opens with.
  FILES: .workflows/v1/specification/tick-core/specification.md:693, .workflows/v1/specification/tick-core/specification.md:714, .workflows/auto-cascade-parent-status/specification/auto-cascade-parent-status/specification.md:146
  DESCRIPTION: §12.2 decides both documents are amended, not left to supersession: "Both documents carry a point that is plainly and load-bearingly wrong, so both are amended." Neither document has been touched by this work unit — `git log` on both returns only `9d5c4aae chore(workflows): reorganize workflow files into per-unit directories`, and neither carries a Corrigenda entry. This is the only §12 deliverable still outstanding; §12.1's README corrections are complete and pinned by `readme_samples_test.go`. The plan deliberately routes these two amendments outside task work (`phase-1-tasks.md:191`, `review-traceability-tracking-c2.md:21-30`: "never by this task"), so the absence is not a task defect — it is a session-route step that has not run. The route is demonstrably live in this phase: this work unit's own specification has received seven corrigenda during implementation.
  RECOMMENDATION: Run the corrigendum route for both documents before the work unit closes, honouring the two bounds §12.2 sets: on `tick-core`, amend principle 3 and its worked description block and stop there — the `task{…}` and `stats{…}` headers in the same passage are left to supersession; on `auto-cascade-parent-status`, replace the arrow-and-`(auto)` toon cascade form and leave its requirement that unchanged terminal children be shown alongside a cascade standing (§7.6). Each amendment presented and confirmed, the wrong claim replaced in place, a dated corrigendum entry recording what the document used to claim and what is true instead, and the document re-indexed. No code change.

- FINDING: The JSON full-graph dependency document reports non-roots, and non-existent tasks, under `roots`
  SEVERITY: medium
  FAILURE: An agent parsing `tick dep tree --json` reads `roots[*].task.id` as the set of tasks that block others and are not themselves blocked — the meaning the key carries and the meaning the command had before this work. In a project holding a dependency cycle, `roots` now lists a task that is blocked; in a project holding a dangling blocker, it lists an ID that names no task at all. The agent starts work on a blocked task, or runs `tick show tick-ghost` and gets "task not found". Nothing in the document distinguishes the two cases.
  FILES: internal/cli/json_formatter.go:341, internal/cli/json_formatter.go:388
  DESCRIPTION: §8's corrigendum decides the full-graph *edge list* covers every participant, not only those a root reaches, and §4.2 binds JSON to toon on the `changed` list and the empty dep-tree form. `formatFullDepTreeJSON` implements the widened coverage by feeding `result.fullGraphTrees()` — roots plus `Unrooted` — into the field still keyed `roots`. In toon the same widening is honest: `dep_tree[N]{from,to}` claims only that an edge exists between two IDs. In JSON the key makes a claim about what its contents are, and that claim is now false on precisely the two states §8's corrigendum was written for. Verified against a built binary: a two-task cycle yields `"roots"` holding a blocked task, and a task blocked by a missing ID yields `"roots"` holding `{"id":"tick-ghost","title":"","status":""}`. The README documents the discrepancy (`README.md:321`, "the JSON `roots` array carry as edges and nodes"), which is a rule learned outside the output — the thing §1 opens this work to delete. The precedent for correcting a JSON key whose type or meaning misleads is this work's own `blocked_by` → `blocker` corrigendum.
  RECOMMENDATION: Name the key for what it holds — every full-graph tree, rooted or not — rather than for what it used to hold, and record the shipped-output change through the corrigendum route as the `blocker` rename was. Confine the change to `jsonDepTreeFull`: toon and pretty do not read it, and the JSON conformance driver already decodes the key through `jsonConformanceListKeys`.

- FINDING: A dependency participant that names no task is rendered as a task with blank title and status
  SEVERITY: medium
  FAILURE: In a project carrying an orphaned dependency — a state `internal/doctor/orphaned_dependency.go` exists to report — `tick dep tree` now draws a tree line reading `tick-ghost   ()` in pretty and emits `{"id":"tick-ghost","title":"","status":""}` in JSON. A human reads the line as a rendering bug; an agent reads the object as a task that exists and has no title, and acts on it. Before this work the ID could not reach either rendering, so both outputs are new.
  FILES: internal/cli/dep_tree_graph.go:228, internal/cli/pretty_formatter.go:422, internal/cli/json_formatter.go:361
  DESCRIPTION: §8's corrigendum requires the full-graph *edges* of a dangling blocker to reach the output, and §4.1's second corrigendum requires pretty to draw the participants no root reaches "through the existing tree rendering with no new visual form". Neither decides how a participant that is not a task is presented as a node. `buildUnrootedTrees` seeds `DepTreeTask{ID: id}` and upgrades it from `taskIdx` only when a record exists, so a dangling blocker keeps zero-valued Title and Status; `writeDepTreeTaskLine` then formats `%s%s  %s (%s)` over the blanks and `toJSONDepTreeNodes` copies them through. The toon edge list is unaffected — it carries IDs only — so the gap is confined to the two node-shaped renderings, which assert a task exists where only an ID does. Verified against a built binary.
  RECOMMENDATION: Take the presentation decision rather than let the zero value stand for it: either confirm through the corrigendum route that a non-existent participant renders with blank title and status, or keep the node-shaped renderings to participants that resolve to a task while leaving the edge coverage §8 requires intact. Either way the answer belongs in the specification, since §8 and §4.1 were both amended for this exact class of graph and neither reaches it.

COMMENT_CORRECTIONS:
- internal/cli/format.go:231 — the comment omits the constraint that makes the field safe: only pretty reads Message, and §8 requires toon and JSON never to
  OLD: 	// Message for edge cases (e.g., "No dependencies found.")
  NEW: 	// Message is the no-dependencies sentence. Only the pretty formatter renders it;
	// the machine formats answer an empty graph with the emptied document instead.

SUMMARY: The code conforms closely to the specification — §5 through §11 all check out against a built binary, including every branch of §9's field selection, §10's marker semantics and §2.2's migrate trim — but §12.2's amendments to the `tick-core` and `auto-cascade-parent-status` specifications have not been made, leaving the superseded output shapes live in the knowledge base. Two smaller drifts sit where §8's widened dep-tree coverage meets the JSON and pretty renderings: the `roots` key now carries non-roots, and a participant that names no task is dressed as a task with blank fields.
