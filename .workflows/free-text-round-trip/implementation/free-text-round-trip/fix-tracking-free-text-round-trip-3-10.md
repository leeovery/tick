## Attempt 1

ISSUES:
- `/Users/leeovery/Code/tick/README.md:281` — the change makes the README's dep-tree prose false. It tells a user the pretty tree draws only root tasks and that the machine formats "also" carry what no root reaches; after this task the terminal carries them too. A user reading it believes `tick dep tree` in a terminal hides a cycle's members, which is the precise belief this task exists to destroy. Not caught by the README guard test (`/Users/leeovery/Code/tick/internal/cli/readme_samples_test.go` compares fenced samples, not prose), and the fenced pretty sample is a rooted chain so it still matches.
  FIX: Replace the first two sentences of README.md:281 so all three formats are described as covering every participant.
  OLD: `Full graph covers every task that participates in a dependency, plus a summary line. The pretty tree draws root tasks (tasks that block others but aren't blocked themselves) with their downstream chains; the TOON edge list and the JSON `roots` array also carry the tasks no root reaches — a cycle's members, or a task blocked by an ID that no longer exists.`
  NEW: `Full graph covers every task that participates in a dependency, plus a summary line. All three formats cover every participant: the pretty tree draws root tasks (tasks that block others but aren't blocked themselves) with their downstream chains, then the tasks no root reaches — a cycle's members, or a task blocked by an ID that no longer exists — which the TOON edge list and the JSON `roots` array carry as edges and nodes.`
  CONFIDENCE: high

NOTES:
- The review's second issue — §4.1's residual clauses "Pretty's tree rendering is untouched" and "It has arisen once" being falsified by this task — was taken by the orchestrator via the specification's corrigendum route rather than routed here, per the review's own ALTERNATIVE. §4.1 now records that the exception arose twice and that pretty's tree draws the participants no root reaches; the corrigendum entry is appended and the document re-indexed. No action is owed by this fix round.
- A dangling blocker renders in the terminal as `tick-ghost1   ()` — id, then an empty title and empty status parenthetical, because the bare-id node carries no title or status. It is the existing line writer applied to more input, which is what the task specified. Giving it a distinguishing form (e.g. a `(missing)` marker) is a new visual form and so excluded by §4.1 and by the task's own wording — recorded so the decision is not defaulted.
- Reverting `dep_tree_graph.go:170` back to `len(roots) == 0` alone fails no test: pretty guards on `len(trees) == 0`, and nothing else reads `Message` on the full-graph path, so a wrong `Message` is currently unobservable. One line at `/Users/leeovery/Code/tick/internal/cli/dep_tree_graph_test.go:599-613` (`result.Message != ""` for the cycle fixture) would pin it. Not raised as an issue: the observable behaviour is fully covered by the e2e command tests.
- No test covers a graph holding both roots and unrooted participants. Exercised manually: a rooted chain, a cycle and a dangling blocker in one project render as three trees followed by `2 chains, longest: 2, 3 blocked`, matching the toon document's four edges and identical counts.
