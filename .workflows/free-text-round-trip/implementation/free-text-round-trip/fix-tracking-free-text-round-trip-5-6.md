## Attempt 1

ISSUES:
- `README.md:641` — the closing sentence claims the marker is never required and that dash-leading text is read as text without it. That is true for `note add` and false for `create`, the command whose example sits three hundred lines above and uses `--` for exactly this reason. Verified against the built binary: `tick create "- fix the header"` → `Error: unknown flag "- fix the header" for "create". Run 'tick help create' for usage.` (exit 1), and `tick create "--dry-run support"` fails identically. `create` has no `flagScanLimit` entry (`internal/cli/flags.go:102-104`), so `ValidateFlags` scans its title. A reader following the Global Flags section hits an error the README told them would not happen — the §12.1 defect ("live documentation … describing output the tool does not produce") reintroduced by the fix for it.
  FIX: replace the second sentence so the bare form is scoped to `note add` and `create`'s dependence on the marker is stated. Exact, mechanically applicable:
  OLD: Flags come before `--`; everything after it is text, including an argument that spells a global flag exactly, so `tick note add tick-a1b2 -- --json` stores the literal note `--json`. The marker is recommended for free text that may begin with a dash, not required — text that does not spell a flag is read as text without it.
  NEW: Flags come before `--`; everything after it is text, including an argument that spells a global flag exactly, so `tick note add tick-a1b2 -- --json` stores the literal note `--json`. On `note add` the marker is recommended rather than required — dash-leading note text that does not spell a global flag is read as text without it. `create` checks its title against its own flags, so a dash-leading title needs the marker.
  This keeps both strings the new prose subtest requires (`--` and `--json` in backticks), so `TestREADMEDocumentsEndOfFlagsMarker` stays green, and it satisfies the task's edge case (does not claim dash-leading note text needs the marker) without the false generalisation.
  ALTERNATIVE: delete the "recommended, not required" clause entirely and let the create/note examples carry the guidance. Cheaper, but it drops the task's explicit edge-case requirement that the docs not present `--` as required, so the reviewer does not recommend it.
  CONFIDENCE: high

NOTES:
- The new subtest "it registers no flag for the marker" (`internal/cli/help_test.go:517-529`) re-asserts what `end_of_flags_test.go:231-247` already asserts, minus the `globalFlagSet` check. The task body asked for it by name, divergence between the two could not be silent, and finding-floor.md puts test-file duplication out of scope — observation only, no action requested.
- `readmeSection` cuts at the next heading of the same level, so a `###` section that happens to be the last one inside its `##` section would swallow following content. Harmless for `### create`, `### note` and `### show` today; worth knowing if the helper is reused.
- `tick help --all`'s global-flags line now ends with a bare `--` token (`Global flags: … --version/-V --`). That is what the task asked for and what the agreement test pins; it reads a little cryptically without a description, which the one-line format cannot carry.
- The Global Flags block is a section-wide claim, so it implicitly covers `migrate`, which is banked (post-marker text still read as flags there). The executor correctly avoided naming `migrate`; flagged so the banked consolidation knows this paragraph is the place the claim lands.
- Coverage gap named but not blocking: the prose subtest asserts only that the Global Flags prose contains the strings `--` and `--json` in backticks, so it cannot detect that the paragraph makes a false claim — which is how this issue survived a green suite. Prose accuracy is not mechanically pinnable here; no extra test is requested.
