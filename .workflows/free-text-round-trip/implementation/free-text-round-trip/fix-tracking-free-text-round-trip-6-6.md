## Attempt 1

ISSUES:
- `internal/cli/toon_formatter_test.go:1413-1439` — `"it leaves unfiltered output unchanged"` compares the whole rendered toon task-detail document (`f := &ToonFormatter{}`, `f.FormatTaskDetail(richDetail())`) against a 24-line golden string literal. This is the single clearest instance of what criterion 1 forbids and of the mechanism §11 names: "the output equals this blob". The two stated greps do not reach it because it is spelled `result != want` against a raw string literal, so a passing grep is not evidence the sweep was complete.
  FIX: delete `want` (1413-1436) and the `result != want` comparison (1437-1439). Keep the `assertToonKeySet` call at 1440-1442 and add decoded value assertions beneath it using the helpers this file and `toon_decode_test.go` already carry: `assertToonFields` for the scalars (`id`, `title`, `status`, `priority`, `type`, `parent`, `created`, `updated`, `closed`, `description`), `assertToonRelatedRow` for `blocked_by` and `children`, `assertToonStringList` for `tags` and `refs`, `assertToonNoteRows` for `notes` — the same set `TestToonTaskDetailConformance` (`toon_decode_test.go:222-238`) uses.
  ALTERNATIVE: if the subtest's real purpose is "an unfiltered detail still renders its sections in the documented order", keep it narrow — `assertToonKeySet` plus a chain of `assertToonKeyOrder` calls (`toon_formatter_test.go:1291`) over adjacent section keys — and let `TestToonTaskDetailConformance` carry the values. Reviewer recommends the first: the value assertions are what fail when a section's content is wrong, and the key-set call already pins the complement.
  CONFIDENCE: high

- `internal/cli/toon_formatter_test.go:1323-1326` — `"it renders selected scalars in output order"` compares a whole rendered filtered document against `"title: Add retry to the sync worker\nstatus: in_progress"`. A whole-output comparison, and the property it exists to check (ordering) already has a decoded-adjacent helper in this file.
  FIX: replace with `assertToonKeyOrder(t, result, "title:", "status:")` plus `assertToonKeySet(t, decodeToonDoc(t, result), "title", "status")` and `assertToonFields` for the two values.
  CONFIDENCE: high

- `internal/cli/toon_formatter_test.go:991-1003` and `internal/cli/toon_formatter_test.go:1199-1211` — the `dep_tree[4]{from,to}:` header is pinned by `lines[0] != "dep_tree[4]{from,to}:"` and the four edge rows are then compared line-by-line against rendered text literals (`"  tick-aaa111,tick-bbb222"` and the rest). These are rendered toon output compared against string literals, they are not count-zero headers, and decoding expresses them completely — the count is the row count and the order is the slice order. The task's own grep vocabulary names `dep_tree[` as a sweep target; the grep misses these only because they are spelled as line comparisons rather than `Contains(stdout, …)`.
  FIX: replace each block with `assertToonEdgeRows(t, decodeToonDoc(t, result), "dep_tree", []toonEdgeRow{{From: "tick-aaa111", To: "tick-bbb222"}, …})`, exactly as `toon_formatter_test.go:1144` and `:1156` already do for `blocks`/`blocked_by`. `assertToonEdgeRows` asserts length and per-index from/to, so both the count and the order survive.
  CONFIDENCE: high

- `internal/cli/toon_decode_test.go:284-290` (`assertChangedHeader`, called at `:303` and `:322`) and `internal/cli/toon_formatter_test.go:680-682` (`Contains(result, "notes[2]{index,text,created}:")`) — count-N section headers retained as text assertions. Criterion 5 says the retained text assertions are exactly the count-zero headers, and the executor applied precisely this reasoning when it converted the count-two `blocked_by[2]{id,title,status}:` pin at `toon_formatter_test.go:155` ("decoding can express that one"). It expresses these too: both call sites of `assertChangedHeader` already assert the decoded row count and every column (`toon_decode_test.go:304-314`, `:323-328`), and `toon_formatter_test.go:683-698` already asserts `index`, `text` and `created` on both note rows.
  FIX: delete `toon_formatter_test.go:680-682`; delete `assertChangedHeader` (`toon_decode_test.go:284-290`) and its two calls. Keep the `changedHeader` const — `toon_decode_test.go:364` still needs it for the retained count-zero comparison, which does sit beside its decoded counterpart at `:367-369` and is correctly retained.
  ALTERNATIVE: keep `assertChangedHeader` but have it derive the count from the decoded document instead of from the header text. That preserves the subtest's shape at the cost of leaving a text pin in place; reviewer recommends deletion, for consistency with the `blocked_by[2]` conversion already made.
  CONFIDENCE: medium

- `internal/cli/toon_formatter_test.go:1385-1390` — the conversion of `"it has no trailing blank line when the last selected field is empty"` traded `result != "notes[0]{index,text,created}:"` for a `HasSuffix(result, "\n")` check plus `assertCountZeroSection`. The replaced equality also guaranteed that the empty `description` rendered nothing at all; the new pair does not — a regression emitting `description: ""` after the notes section passes both checks. The two sibling conversions in `list_show_test.go` (`:936`, `:1394`) did add `assertToonKeySet(..., "notes")` for exactly this reason, so the omission here is inconsistent rather than deliberate.
  FIX: add `assertToonKeySet(t, decodeToonDoc(t, result), "notes")` beside the existing assertions, matching `list_show_test.go:936`.
  CONFIDENCE: high

NOTES:
- Three text probes on rendered machine-format output are retained outside the stated count-zero set and are, in the reviewer's judgement, correctly retained — but the task's premise was that the retained set be "inspectable rather than a matter of judgement at each site", so they should be named in the handback rather than left silent. (a) `toon_formatter_test.go:618` `Contains(result, "refs[1]: gh-123")` — the inline-list form of §6.1 is the one thing decoding cannot recover, since an inline list and a dash-list decode identically, and the `assertToonStringList` call on the next line covers only the values. (b) `json_formatter_test.go:839-841` and `:1453-1459` — two-space indentation and absence of tabs in rendered JSON, likewise unrecoverable by decoding. Each is the same class of exemption as the count-zero header: a required shape no decoded assertion can express.
- `list_show_test.go:936` and `:1394` lost the exact-stdout equality, and with it the only assertion that a `--field` toon document terminates with a single newline. §9.2 fixes that byte for the bare value only, so nothing now covers it for the document form. Low value; recorded rather than asked as a change.
- `assertJSONRendersEmptyArray` and the pre-existing `assertJSONEmptyArray` differ by one word and take different argument shapes (rendered string vs. decoded doc + key). The executor flagged the collision and chose correctly, but a reader scanning call sites will not see which is which. `assertRenderedJSONIsEmptyArray`, or having the new one delegate, would read better. Not worth a fix round on its own — fold it in if the file is touched for the issues above.
- Nothing rode this dispatch beyond the enumerated inputs.
