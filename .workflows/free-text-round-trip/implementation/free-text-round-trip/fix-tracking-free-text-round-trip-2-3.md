## Attempt 1

ISSUES:
- `/Users/leeovery/Code/tick/internal/cli/cascade_formatter_test.go:152-157` — the two literal `parsed["transition"]` / `parsed["cascaded"]` guards leave the task's grep criterion false, and they are already subsumed by the stronger exact-key assertion in `changedRows` (`cascade_formatter_test.go:164-166`). The only decode path that misses that stronger check is `TestJSONFormatterCascadeTransition/it_emits_changed_as_an_empty_array_never_null`, which calls `decodeChangedDoc` directly.
  FIX: move the exact-key assertion into `decodeChangedDoc` and delete the two named-key guards — i.e. replace lines 152-157 with `if len(parsed) != 1 { t.Errorf("document has keys %v, want changed only", slices.Sorted(maps.Keys(parsed))) }`, and drop the now-duplicate block at `changedRows` lines 164-166. Every decode path then asserts `changed` is the document's only key, which is strictly stronger than naming two absent keys, satisfies Do-step 4's "assert the absence of `transition` and `cascaded`", and clears the grep. `maps`/`slices` stay imported via the moved line.
  ALTERNATIVE: keep the guards and treat the criterion as met in spirit (no producer of either key remains anywhere). Costs nothing now but leaves a mechanically checkable criterion false, so phase 6's conformance sweep or the phase review re-raises it. Recommend the fix above — it is a net-stronger assertion, not a concession to the grep.
  CONFIDENCE: high

NOTES:
- The executor's BANK entry (derive pretty from `Changed`, delete `CascadeEntry` and the `TaskID`/`TaskTitle`/`OldStatus`/`NewStatus`/`Cascaded` fields) should not be actioned as written: `phase-2-tasks.md:14` and `:51` explicitly require those fields be kept "exactly as they are" because `PrettyFormatter.FormatCascadeTransition` builds its `ParentID`-linked tree from them, and §4.1 of the specification fixes pretty's output as unchanged and names the flattened table as a declined change. The parallel-representation complexity inside `buildCascadeResult` (`transition.go:61-118`) is real, but the consolidation it proposes needs a spec amendment, not a consolidation pass.
- `reopen` is named in the first acceptance criterion but is not exercised end to end with `--json`. All four commands dispatch through the same `handleTransition` → `RunTransition` → `outputStatusChanges` path (`app.go:131`, `transition.go:52`), so coverage is by construction; not worth a fix round.
- The task listed `"it emits auto as a JSON boolean"` and `"it emits no transition or cascaded key"` as named subtests; the executor implemented them as assertions inside `assertChangedRow`/`decodeChangedDoc`, applied to every subtest. That is broader coverage than the named subtests would give, so I did not raise it — but those two test names do not exist in the suite if anything downstream greps for them.
- `TestAllFormattersCascadeEmptyArrays` and its subtest name "all formatters handle empty cascaded" are now stale descriptors: the body asserts a one-row changed list and nothing empty. Test naming is below the finding floor, noted only in case a later task rewrites this file.
- `mergeStatusChanges` (`transition.go:168`) still has no production caller — task 2-1's output awaiting task 2-5. Outside this task's scope.
