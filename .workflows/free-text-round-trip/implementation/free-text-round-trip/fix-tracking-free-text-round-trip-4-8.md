## Attempt 1

ISSUES:
- `README.md:209` — the prose generalises to all five list sections a guarantee only `notes` provides. `--field title,tags.2` renders `tags[1]: backend`, `--field title,children.1` renders a `children[1]{id,title,status}:` row, and neither shows the item's real position; in `--pretty` even the narrowed notes section prints timestamps and no index. §9.3 states the carve-out explicitly ("Only notes carry a real position in the row itself"), and §12.1's whole point is that the README must not describe output the tool does not produce. A caller who narrows `children.3` or reads the pretty form after this sentence looks for a position that is not there.
  FIX: replace the sentence, scoping the index guarantee to `notes` and saying what the other sections do instead.
  OLD: A lone position on `children` or `blocked_by` returns its one-row section instead, because a row is not a value. Inside a multi-name selection a position always returns a section, and each row keeps its real position in the whole section while the count follows the selection: `--field title,notes.2` renders `notes[1]{index,text,created}:` with a row indexed `2`, so the position you read is still the one to act on.
  NEW: A lone position on `children` or `blocked_by` returns its one-row section instead, because a row is not a value. Inside a multi-name selection a position always returns a section, narrowed to the items it names while the count follows the selection. A narrowed `notes` section keeps each row's real 1-based `index`, so `--field title,notes.2` renders `notes[1]{index,text,created}:` with a row indexed `2` and the position you read is still the one to act on. The other sections carry no index column, so a narrowed one hands back the item without its position.
  ALTERNATIVE: keep the general sentence and append one clause naming notes as the only section with an index column. Shorter, but it leaves "each row keeps its real position" standing as the lead claim, which is the part that misleads. The reviewer recommends the replacement above.
  CONFIDENCE: high

- `internal/cli/readme_samples_test.go:441-455` — both subtests use bare substring containment, and `--field` is a substring of `--fields`. Delete the `--field` row from the README table and the subtest named "it documents both field flag spellings" still passes, because "Alias of `--field`" — or `--fields` alone — satisfies it. The acceptance criterion "Both `--field` and `--fields` appear" is therefore unguarded for the singular, and code-quality.md lists substring assertions over deterministic text as an anti-pattern. The README backticks every flag, so an exact-enough token is available.
  FIX: match the backticked spellings. In "it documents both field flag spellings" iterate the two backticked forms; in "it matches the help text for show" wrap each registry name the same way. Both pass against the README as written. While in the helper, use the existing `findCommand("show")` (`internal/cli/help.go:242`) instead of the hand-rolled loop, and filter to `strings.HasPrefix(spelling, "--")` so the code matches its own doc comment ("every long flag name") and the sibling drift test at `internal/cli/flag_validation_test.go:344-352`.
  ALTERNATIVE: a word-boundary regex instead of backticks. It survives a README that stops backticking flags, at the cost of a regex per flag and a dependency on the README never writing `--fields` where it means `--field`. Backticks are simpler and match the README's actual style; the reviewer recommends them.
  CONFIDENCE: high

NOTES:
- The executor's own flag about JSON key order is accurate and the reviewer would leave the README alone: `--field title,status --json` emits `status` before `title` (alphabetical), so "normal output order" holds for toon and pretty only. That ordering is pre-existing JSON formatter behaviour, §9.2 phrases the rule the same way, and the sentence sits in the paragraph introducing the toon sample. Out of this task's scope.
- The Output Formats section's full-detail sample (`README.md:469-489`) shows `tick-a1b2` with `notes[1]`, while the new field-selection sample shows the same ID with `notes[2]`. Deliberate and correct: the alternative would edit a Phase 1-3 sample, which the task forbids.
- §9.6's empty-field rule (an unset field prints what full output prints and exits successfully) is not documented in the `### show` section. The task's Do step 3 does not require it, so this is an observation, not a gap.
- The README/help drift guard now exists for `show` only; every other command section is still unchecked against the help registry. `readmeShowSection` generalises to any command heading if that is ever wanted — new work rather than consolidation, so not banked.
