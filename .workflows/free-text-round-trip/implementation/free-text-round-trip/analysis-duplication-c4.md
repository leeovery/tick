AGENT: duplication
FINDINGS:
- FINDING: The selection's narrowing of list sections is restated in all three formatters
  SEVERITY: medium
  FAILURE: A list section added to the detail document, or a collection paired with the wrong field-name constant in one formatter, leaves that one format rendering the section unnarrowed (or narrowing the wrong one) while the other two narrow it correctly. Nothing fails: the compiler sees a well-typed call, the document still parses, the command still exits 0, and §11's conformance drivers assert only that the document decodes. An agent that runs `tick show <id> --field notes.2` under one format is handed a different note than under another, believes the note it read is the note it asked for, and runs `tick note remove <id> 1` against a position that does not hold it — the exact loss §6.3 introduced the index column to prevent. Noticed only as a deleted note nobody asked to delete.
  FILES: internal/cli/toon_formatter.go:78, internal/cli/toon_formatter.go:83, internal/cli/toon_formatter.go:88, internal/cli/toon_formatter.go:93, internal/cli/toon_formatter.go:98, internal/cli/json_formatter.go:115, internal/cli/json_formatter.go:116, internal/cli/json_formatter.go:117, internal/cli/json_formatter.go:118, internal/cli/json_formatter.go:127, internal/cli/pretty_formatter.go:158, internal/cli/pretty_formatter.go:187, internal/cli/pretty_formatter.go:192, internal/cli/pretty_formatter.go:197, internal/cli/pretty_formatter.go:202
  DESCRIPTION: `selectedItems(detail.X, sel.Positions(fieldX))` is written out fifteen times — once per list section per formatter — and the pairing of a collection with the field name that selects it exists only in those fifteen expressions. The pairing is not type-checked: `detail.Refs` with `sel.Positions(fieldTags)` compiles, and `detail.Notes` taken whole compiles. The one guard over the three formatters, `TestRegisteredFieldRendering` (internal/cli/show_fields_test.go:760), asserts each format renders something non-empty for each registered name; it passes unchanged when a format ignores positions entirely and renders the whole section, which is the failure above. The rendering of each section legitimately differs by format — a toon table, a JSON array, a pretty block — but the narrowing that precedes it is identical in all three and is the part being restated.
  RECOMMENDATION: Resolve the narrowing once. Add an accessor beside `TaskDetail` in internal/cli/format.go — e.g. an unexported `selectedSections` struct carrying the narrowed Tags, Refs, BlockedBy, Children and Notes plus the note positions, and `func (d TaskDetail) selectedSections() selectedSections` applying `d.Fields` — and have `ToonFormatter.FormatTaskDetail`, `taskDetailJSONObject` and `prettyDetailHeader`/`prettyDetailBlocks` read the values they render from it instead of calling `selectedItems` themselves. The field-name-to-collection pairing then lives in one function, beside the existing `showFields` registry that already pairs the names with their value accessors.

- FINDING: The stored-dependency edge list is enumerated twice
  SEVERITY: low
  FAILURE: A change to the edge-enumeration rule made in one function and not the other makes `tick dep tree` and `tick dep tree <id>` report different edges for the same stored dependencies. Both documents parse, both agree internally with their own counts, and each function's tests keep passing, so nothing reports it; an agent reconciling the project view with the focused view is told the store holds two different graphs.
  FILES: internal/cli/dep_tree_graph.go:184, internal/cli/dep_tree_graph.go:196
  DESCRIPTION: `collectStoredEdges` and `collectScopedStoredEdges` both walk `tasks` in slice order and each task's `BlockedBy` in stored order, emitting one `DepTreeEdge{From: dep, To: t.ID}` per entry; the scoped one adds a membership test on each end and nothing else. Their doc comments restate the ordering rule word for word ("tasks in slice order and each task's blockers in stored order"). §8's corrigendum of 2026-09-20 records this rule diverging once already between the full graph and the focused view — five stored dependencies coming back as five rows from one and six from the other — which is what a single enumeration exists to prevent.
  RECOMMENDATION: Keep one enumeration. Have `collectStoredEdges(tasks)` delegate to the scoped form with a scope that keeps every ID (either a `keep func(id string) bool` parameter, or a nil set read as "no scope"), so record order and one-row-per-`BlockedBy` are stated in one function and the scope filter is the only thing the focused form adds.
COMMENT_CORRECTIONS:
- internal/task/transition.go:5 — the worked example prints the arrow line §7.2 replaced with the `changed` table in toon and JSON.
  OLD: // TransitionResult holds the old and new status after a successful transition,
  // enabling the caller to format output like "tick-a3f2b7: open -> in_progress".
  NEW: // TransitionResult holds the old and new status after a successful transition.
SUMMARY: Two duplications clear the floor: the field selection's narrowing of the five list sections is restated in each of the three formatters with nothing holding the field-name-to-collection pairing, and the stored-dependency edge list is enumerated once for the full graph and again for the focused view. One comment correction: a worked example in `TransitionResult`'s doc names output the machine formats no longer produce.
