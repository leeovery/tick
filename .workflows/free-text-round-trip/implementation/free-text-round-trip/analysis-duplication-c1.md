AGENT: duplication
FINDINGS:
- FINDING: Every count-zero toon section header hand-writes the column list its row struct already declares
  SEVERITY: medium
  FAILURE: A later change adds, renames or reorders a column on one of the five toon row structs. The populated branch picks the change up from the struct's `toon` tags; the count-zero branch keeps its literal. `tick done` on a task that cascades then prints `changed[2]{id,title,from,to,auto,owner}:` while `tick done` on a task that moves nothing prints `changed[0]{id,title,from,to,auto}:` — the same section declaring two schemas depending on whether it has rows, which is exactly the branch-on-which-document-arrived defect §7.2 and §8 exist to delete. An agent that learned the schema from an empty document mis-reads the populated one, and notices only at parse time against real data. Nothing in the suite fails: `assertCountZeroSection` (`internal/cli/toon_decode_test.go:161`) compares the empty header against its own copy of the same stale literal, `assertToonFields` (`:58`) only checks the keys it is handed, and the README samples that readme_samples_test does pin against real output are the *populated* ones (`README.md:479`, `:488`, `:515`, `:559`, `:438`, `:343`) — so the loud failure fires on README and the struct, the developer fixes both, and the count-zero literals are the one copy nothing pushes them to touch.
  FILES: internal/cli/toon_formatter.go:53, internal/cli/toon_formatter.go:150, internal/cli/toon_formatter.go:241, internal/cli/toon_formatter.go:308, internal/cli/toon_formatter.go:320
  DESCRIPTION: Five sites state a section's column list as a string literal that must equal the `toon` tags of the row struct the populated branch marshals: `tasks[0]{id,title,status,priority,type}:` (`:53`) against `toonTaskRow` (`:22-28`); `changed[0]{id,title,from,to,auto}:` (`:150`) against `toonChangedRow` (`:139-145`); `%s[0]{from,to}:` (`:241`) against `toonEdgeRow` (`:165-168`); `%s[0]{id,title,status}:` (`:308`) against `toonRelatedRow` (`:31-35`); `notes[0]{index,text,created}:` (`:320`) against `toonNoteRow` (`:38-42`). The literals are not gratuitous — `toon.MarshalString` of an empty typed slice emits `changed[0]:` with no columns at all (verified against `toon-go` in this tree), so the library cannot produce the count-zero header §8 requires, which is why each site hand-builds it. That leaves the schema written twice per section with nothing tying the copies together, and this work extended the pattern rather than closing it: `buildChangedSection` is a new sixth copy and `buildNotesSection`'s header gained `index` by hand-editing the literal beside the struct field.
  RECOMMENDATION: Replace the five literals with one generic helper in `toon_formatter.go` — `emptyToonSection[T any](name string) string` — that reads `T`'s `toon` struct tags in declaration order via reflection and renders `name[0]{…}:`. Each site then calls it with the same row type its populated branch marshals (`emptyToonSection[toonChangedRow]("changed")`, `emptyToonSection[toonRelatedRow](name)`, and so on), so the column list exists once, in the struct. Leave the literals in the test assertions and the README samples: with the production copy derived, those become the independent pin that fails loudly when a column moves, which is the behaviour the empty branch does not have today.
COMMENT_CORRECTIONS:
- internal/cli/helpers.go:99 — restates the name and the single line beneath it, carrying nothing the code cannot
  OLD: // outputStatusChanges writes a command's status changes to stdout.
  NEW:
SUMMARY: One duplication finding: the count-zero toon section headers restate, as string literals, the column lists their row structs already declare, and the copies can diverge with no test, README sample or compile error noticing. Three near-duplicate families were considered and dropped against the floor — the per-format detail projection across `toon_formatter.go`/`json_formatter.go`/`pretty_formatter.go` (guarded by `TestRegisteredFieldRendering`, and consistent today), the fifteen `selectedItems(detail.X, sel.Positions(fieldX))` pairings (a typo risk rather than a rule that can silently diverge, with no type-concrete consolidation available), and `showListSections`' claim to mirror the toon document's section order (drift is silent but only reorders which section an out-of-range error names).
