AGENT: standards
FINDINGS:
- FINDING: A refused value carried by a related or cascaded task names the document's subject instead of the task that carries it
  SEVERITY: medium
  FAILURE: A child task whose title carries an escape sequence (§3.1: "an escape sequence inside pasted terminal output is the everyday way") makes both `tick show <parent>` and `tick done <parent>` fail with a diagnostic naming the *parent* — a task whose every value encodes cleanly. Reproduced against a binary built from HEAD: `tick --toon show tick-bbe517` → `Error: cannot encode section children of task tick-bbe517 as TOON: toon: unsupported control character U+001B in string`, and `tick --toon done tick-bbe517` → `Error: cannot encode section changed of task tick-bbe517 as TOON: …`. The offending task, `tick-c8aaf3`, is named nowhere. An agent acts on the ID it was handed, re-runs `tick show` on it, gets the identical error, and is left with the routes §1 opens this work to delete — switching format or reading `.tick/tasks.jsonl` — while believing it has already located the culprit. Naming the wrong task is worse than §3.1's own fallback of naming none, because the ID reads as an answer.
  FILES: internal/cli/toon_formatter.go:392, internal/cli/toon_formatter.go:300, internal/cli/toon_formatter.go:150, internal/cli/toon_formatter.go:164, internal/cli/toon_refusal_test.go:231
  DESCRIPTION: §3.1 as amended requires the diagnostic to name "the field or section it could not encode and the task that carries the refused value", with "the section name alone stand[ing]" only "where the offending row cannot be identified". Phase 9-2 built exactly that mechanism — `encodeToonSectionIdentified` plus `sectionRefusal` re-encode each row alone on the error path and set `toonEncodeError.taskID` to the first row that fails — but wired it to `FormatTaskList` only. `buildRelatedSection` (`blocked_by`, `children`) and `buildChangedSection` still call the identity-less `encodeToonSection`, and `refusalForTask` then overwrites whatever `taskID` the error holds with the document's subject (`&toonEncodeError{part: refusal.part, taskID: taskID, err: refusal.err}`), so even a future identity would be discarded. Every row of all three sections carries a task ID, so the offending row *can* be identified in each case — the condition §3.1 attaches to the section-name-only fallback is not met. The gap is pinned rather than accidental: `toon_refusal_test.go:231` ("it names the document's subject when the refused value sits on a cascaded task") asserts the cascaded child's ID is absent from stderr, following step 4 of the phase-9 staged task; the consolidation settled it, the specification sentence it is measured against did not.
  RECOMMENDATION: Pass a row identity to the three sections whose rows are other tasks — `buildRelatedSection` (`func(r toonRelatedRow) string { return r.ID }`) and `buildChangedSection` (`func(r toonChangedRow) string { return r.ID }`) — and stop `refusalForTask` clobbering a `taskID` the refusal already carries (override only when `refusal.taskID == ""`), so a document subject still names sections whose rows have no identity of their own, such as `notes`. Update the cascade subtest to assert the cascaded child is named. If the settled behaviour is instead to be kept, it needs a §3.1 corrigendum bounding task-naming to the task the row belongs to only in a task list, because the body sentence as it stands is unqualified.

- FINDING: The README's refusal paragraph describes the pre-9-2 diagnostic, which names no task for a list
  SEVERITY: low
  FAILURE: A reader of the README's TOON section learns that a refused value is reported "naming the field or section it could not encode — and the task, where the document covers one". `tick list`, `tick ready` and `tick blocked` cover many tasks, so that sentence tells the reader the ID will not be in the message; the shipped behaviour since Tfree-text-round-trip-9-2 names it (`assertRefused(t, stdout, stderr, exitCode, "tasks", refused)`, `toon_refusal_test.go:163`). A reader who believes the document goes looking for the row by bisection or in `.tick/tasks.jsonl` — the exact route §3.1's amendment was written to remove — with the answer already sitting in the error they dismissed.
  FILES: README.md:494
  DESCRIPTION: The paragraph landed with Tfree-text-round-trip-9-1, whose rule was "the task where the document covers one". Tfree-text-round-trip-9-2 moved that rule (§3.1's final corrigendum: "the diagnostic [names] the task carrying the refused value in a list as well, with the section name alone standing only where no single row can be attributed") and touched `internal/cli/toon_formatter.go` and two test files only, leaving the README describing the superseded behaviour. §12.1 holds the README to what the tool produces — "It is live documentation someone reads to learn the tool… so leaving it describing output the tool does not produce is shipping a defect" — and the new input surface and the changed samples were corrected under exactly that rule.
  RECOMMENDATION: Replace the restriction clause so it states both halves of the shipped rule: the diagnostic names the field or section, and the task carrying the refused value — the document's subject where it covers one, the offending row where it is a list — with the section name alone standing only where no row can be attributed. Keep the sentence that follows, which is accurate.
COMMENT_CORRECTIONS:
- internal/task/transition.go:6 — the doc's worked example is the arrow line §7.2 replaced with the `changed` table, so it names output the tool no longer produces
  OLD: // TransitionResult holds the old and new status after a successful transition,
  // enabling the caller to format output like "tick-a3f2b7: open -> in_progress".
  NEW: // TransitionResult holds the old and new status after a successful transition.
SUMMARY: The shipped behaviour conforms to §2 through §12 on every branch re-checked against a binary built from HEAD — the detail document's named fields, inline tags/refs and quoted description, the `changed` table's always-present and at-most-once rules, the stored-relation edge lists with `missing` for a dangling participant, the field-selection grammar with its three error classes, the end-of-flags marker and its `--flag=value` repair, the migrate trim, and the toon/JSON conformance drivers with the awkward-fixture round trip; `gofmt`, `go vet`, `go test ./...` and `golangci-lint` are all clean. Two things drift: phase 9's row-identity mechanism was wired to the task list alone, so a refused value on a child or a cascaded task names a task that does not carry it, and the README's refusal paragraph still describes the diagnostic as it stood before that phase's second task.
