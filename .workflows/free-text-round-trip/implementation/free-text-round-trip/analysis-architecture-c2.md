AGENT: architecture
FINDINGS:
- FINDING: The full-graph edge list is re-derived from tree walks, so a converging path publishes the same dependency twice
  SEVERITY: medium
  FAILURE: An agent runs `tick dep tree` and builds a dependency graph from the `dep_tree[N]{from,to}` rows. Where two dependency paths converge above a further-blocked task it records an edge that exists once as two edges, and answers "what does C block" with 2 where `tick show C` answers 1. Reproduced against a binary built from HEAD. Ordinary CLI usage, no file editing: `create Alpha`, `create Alpha2`, `create Beta --blocked-by=Alpha,Alpha2`, `create Gamma --blocked-by=Beta` returns `dep_tree[4]` carrying `Beta,Gamma` twice beside `chains: 1`. The seeded-tree path this work added widens it to a second class: a task carrying a blocker ID no record matches, above an existing chain (`Alpha→Beta→Gamma` plus `tick-ghost` blocking Beta) returns `dep_tree[4]` with `Beta,Gamma` twice, and pretty draws the whole `Beta→Gamma` subtree a second time under `tick-ghost   (missing)`. In a cycle the same re-walk inflates the summary figure the terminal and the agent share: a two-task cycle alone reports `longest: 2`, and adding one unrelated dangling blocker reports `longest: 3` off a path that revisits Alpha. Noticed when the agent's dependency count disagrees with the task records — or not at all, the agent simply acting on a graph carrying phantom edges.
  FILES: internal/cli/toon_formatter.go:183-190, internal/cli/dep_tree_graph.go:129-158, internal/cli/dep_tree_graph.go:220-241
  DESCRIPTION: The edges are the source data — every `t.BlockedBy` entry is exactly one edge — but the toon document derives them by flattening `result.Trees`, a structure built by walking down from each root and each seed. A tree walk cannot represent a DAG without repeating the shared part, so flattening it back into an edge list reproduces each convergent subtree once per path into it. §8 required the full-graph list to cover every participant, and the implementation met that by adding `buildSeededTrees` plus the `emitted`/`collectTreeIDs` bookkeeping to cover what the root walk misses; that whole apparatus exists only because the edge list is taken from the trees rather than from the relation. `buildSeededTrees` consults `emitted` to choose a seed and then ignores it for the duration of the walk, so a seed above already-emitted ground re-covers all of it. Both halves of one document then disagree in the way §8's corrigendum set out to prevent: the summary says one chain while the edge list lists four edges for three dependencies, and the reader is back to a rule learned outside the output ("dedupe the rows yourself"). The two-root case predates this work; the seeded case is new, and no test covers a seed whose walk overlaps an emitted tree — the dangling-blocker fixture (`internal/cli/dep_tree_test.go:223`) carries a single blocked task with nothing beneath it.
  RECOMMENDATION: Derive the full-graph rows from the relation that holds them. `BuildFullDepTree` already holds every `BlockedBy` entry; one pass yields exactly one `{from,to}` row per stored dependency, covers every participant by construction, and cannot repeat — so the toon edge list stops depending on the seeding machinery entirely. Carry them on `DepTreeResult` (an `Edges []DepTreeEdge` field) and have `ToonFormatter.formatFullDepTree` render those instead of walking `result.Trees`; leave `Trees` to the node-shaped renderings, where drawing a shared subtree under each of its blockers is a drawing rather than a duplicated fact. Row order moves from walk order to record order, so the dep-tree edge assertions need their expected rows reordered. If pretty's repeated subtree is judged worth fixing alongside, prune the seeded walk at nodes `emitted` already holds so a seed renders its already-drawn child as a leaf.

COMMENT_CORRECTIONS:
- internal/cli/format.go:258 — restates the clause above it and names its embedders, a claim ordinary additive change falsifies
  OLD: // Embedded by ToonFormatter and PrettyFormatter.
  NEW:

SUMMARY: The field-selection module, the end-of-flags split, the merged `changed` table and the conformance inventory compose cleanly, and the cycle-1 seams (note index ordering, the JSON `trees` key, the source-text guard) are closed. One seam does not hold: the full-graph edge list is flattened out of a tree walk rather than read off the `BlockedBy` relation, so convergent paths — and the seeded trees this work added — publish the same dependency more than once.
