AGENT: architecture
FINDINGS:
- FINDING: The note index the detail document publishes is not the index `note remove` consumes
  SEVERITY: high
  FAILURE: An agent reads `notes[2]{index,text,created}:`, decides row 2 is wrong, and runs `tick note remove <id> 2`. On a task whose stored notes are not in non-decreasing `created` order, a different note is deleted, silently, exit 0. Reproduced against the built binary: a task whose two JSONL notes carry stamps in descending order renders `1,"SECOND in file, earlier stamp"` / `2,"FIRST in file, later stamp"`, and `note remove <id> 1` deletes "FIRST in file" and leaves "SECOND in file". Noticed only when the agent re-reads the task and finds the note it meant to keep gone.
  FILES: internal/cli/show.go:169, internal/cli/note.go:130-134, internal/cli/toon_formatter.go:319-332, internal/cli/show_fields.go:282-295
  DESCRIPTION: §6.3's index column exists to give a note a handle an agent can pass to `note remove`, so the two sides must agree on what "position N" means — and they are computed by two different rules in two modules. `queryShowData` reads notes `ORDER BY created ASC, rowid ASC`; `RunNoteRemove` indexes `tasks[i].Notes`, the JSONL slice. `rowid` follows JSONL order on every rebuild, so the tiebreak makes the two agree whenever the stamps are non-decreasing — which is exactly the case the equal-timestamp test covers — but `created ASC` is the dominant key, so any task whose stored notes are out of chronological order makes the published index name a different note than the one removal deletes. Out-of-order stamps are reachable without anything exotic: `tasks.jsonl` is the shared, hand-editable source of truth, and clock skew between two machines appending to the same committed file produces them. The same ordering feeds `--field notes.N` and `ValidatePositions`, so the narrowed section and the range error inherit the mismatch. The ordering that makes the contract true by construction is the log's own: rowid alone is the JSONL position, which is what removal counts.
  RECOMMENDATION: Order the notes query by `rowid ASC` alone in `queryShowData`, so the index column, the `notes.N` selector and `note remove` all address the same position by construction rather than by the stamps agreeing. The equal-timestamp case the current tiebreak was added for is subsumed. Keep an assertion that a task whose notes carry descending stamps renders them in stored order and that removing the published index removes that row.

- FINDING: The full-graph JSON document labels non-root trees `roots`
  SEVERITY: medium
  FAILURE: A consumer of `tick dep tree --json` reads `roots` as "tasks nothing blocks" — which is what the key says and what it held until this change — and picks one as the place work can start. In a project carrying a dependency cycle or a blocker ID no task record matches, `roots` now also contains blocked participants, so the consumer starts or reports a task that `tick show` says is blocked. Verified against the built binary: a two-task cycle returns `"roots"` carrying Alpha, which is blocked by Beta.
  FILES: internal/cli/json_formatter.go:339-345, internal/cli/json_formatter.go:388, internal/cli/format.go:217-239, README.md:321
  DESCRIPTION: §8's requirement that the full graph cover every participant was implemented by seeding extra trees from participants no root reaches. `DepTreeResult` keeps that distinction in two fields, and all three formatters then discard it through `fullGraphTrees()`. For toon that is harmless — the document is an edge list and claims nothing about rootness — and pretty draws a tree with no label. JSON is the one format whose key asserts the property that is no longer true of its contents, and the document carries no discriminator to recover it from. The README now has to explain the quirk in prose (`README.md:321`: "the JSON `roots` array carry as edges and nodes"), which is a decoding rule learned outside the output — the class of defect §1 opens this work for, and the same defect this work already repaired once by renaming `blocked_by` to `blocker` on the dependency-change document rather than letting one key mean two things. The split in the domain type is meanwhile bought and never spent: no consumer reads `Roots` or `Unrooted` separately.
  RECOMMENDATION: Rename the JSON key to one that claims only what the value holds (`trees`), and collapse `Roots`/`Unrooted` into the single field every consumer actually reads, leaving the root-versus-seeded distinction local to `BuildFullDepTree` where it is used. Drop the README sentence that exists to explain the mislabel. If the distinction is judged worth publishing instead, keep the two fields and give the seeded trees their own key rather than folding them into `roots`.

- FINDING: A suite guard asserts on the source text of sibling test files
  SEVERITY: low
  FAILURE: A maintainer renames a subtest or moves an assertion and the suite fails naming a file that is not the one they edited, with no product behaviour involved; conversely a maintainer empties the body of one of the named tests and the guard still passes, so the coverage it exists to protect is gone while the suite stays green. Noticed as a failing test nobody can map to a behaviour, or as coverage quietly lost.
  FILES: internal/cli/conformance_test.go:1310-1338
  DESCRIPTION: `TestConformanceScopeBoundary` reads four sibling test files and asserts that named function names and subtest strings still appear in them. It checks nothing the tool does: it pins test identifiers, which are the most refactor-prone text in the suite, and it is satisfied by a name with no assertions behind it. It also encodes a workflow concern — "an earlier phase's assertions must not be deleted by a later one" — into the shipped suite, where it will outlive the phases that motivated it. The suite already has the guard that matters: the conformance drivers decode every document in the inventory, so a deleted assertion that mattered shows up as a document nobody checks, not as a missing string.
  RECOMMENDATION: Delete the test. The assertions it names are carried by their own files; if one of them pins something the conformance drivers do not, express that as an assertion in the driver rather than as a grep over source text.

COMMENT_CORRECTIONS:
- internal/cli/helpers.go:99 — restates the signature of the one-line wrapper beneath it
  OLD: // outputStatusChanges writes a command's status changes to stdout.
  NEW:

SUMMARY: The field-selection module, the end-of-flags split and the conformance inventory compose cleanly, and the status-change table is properly derived rather than re-implemented per formatter. Two seams do not hold: the note index the detail document publishes is computed by a different ordering than the one `note remove` consumes, which deletes the wrong note where the two disagree, and the full-graph JSON document keeps a key named `roots` after filling it with trees that are not rooted.
