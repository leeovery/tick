AGENT: architecture
FINDINGS:
- FINDING: The toon encoder's refusal is turned into a well-formed document that denies the data exists
  SEVERITY: high
  FAILURE: A stored title, description or note carrying any C0 control character other than tab, newline or carriage return — an ANSI escape inside pasted terminal output is the everyday way to get one — makes the agent-default toon output answer with a document that says the data is absent, exit 0, no diagnostic anywhere. Reproduced against a binary built from HEAD, ordinary CLI input only:
    `tick create "Colour paste" --description $'red \x1b[31mword\x1b[0m done'` then `tick show <id>` prints the header and the empty sections with no `description:` line at all, while `tick show <id> --json` returns the description intact;
    `tick create $'Colour \x1b[31mtitle\x1b[0m'` then `tick show <id>` prints a document with no `id`, no `title`, no `status` — only `blocked_by[0]`, `children[0]`, `notes[0]` — so the document identifies no task;
    `tick note add <id> $'bell \x07 note'` then `tick show <id>` prints `notes[0]:`, telling the agent the task carries no notes, which is also what `--field notes` and the `notes.N` range check then report;
    one such task anywhere in the project makes `tick list` print `tasks[0]:`, so the whole project reads as empty.
  Noticed as an agent acting on absent data — re-adding a description it already has, concluding a project is empty, or going back to `.tick/tasks.jsonl` — and never as an error. Every one of these documents parses, so §11's conformance drivers pass on them and the suite is green.
  FILES: internal/cli/toon_formatter.go:271, internal/cli/toon_formatter.go:277-286, internal/cli/toon_formatter.go:333, internal/cli/toon_formatter.go:52-65, internal/cli/toon_formatter.go:301-316
  DESCRIPTION: The encoding seam this work built — `encodeToonFields` for §5's top-level named fields, `encodeToonSection`/`emptyToonSection` for §6 and §8's sections, composed by `joinToonSections` — has exactly one error path, and it substitutes a well-formed falsehood for the failure. `encodeToonFields` returns `""` and `joinToonSections` then drops the section, so one refused value deletes the entire top-level header block from the document; `encodeToonSection` returns `name[0]:`, a count-zero header asserting the section holds nothing when it holds rows. Both are silent and both exit 0. The pre-change header site returned `"task:"`, which was at least visibly broken; the reshape made the same failure invisible. That inverts the guarantee the work exists to make. §1's thesis is output that claims to be machine-readable and is; §2.2's bar is that a value read out of `tick show` is the stored bytes; §11's verification checks only that what is emitted parses — and the substituted document parses, so the conformance inventory can never see it however many branches it covers. The trigger set is the plain C0 range the library refuses (every codepoint below U+0020 except `\t`, `\n`, `\r`), nothing on any write path rejects or normalises it — `create`, `update`, `note add` and `tick migrate` all store it — and no test in the suite carries such a value, the awkward fixture included.
  RECOMMENDATION: Stop substituting. Carry the encoder's error out of the three helpers rather than discarding it, so `FormatTaskList`, `FormatTaskDetail` and the section builders surface it and the command exits non-zero naming the task and the field, exactly as an unresolvable ID already does. Whether it travels as a second return value on the `Formatter` methods or as state on `ToonFormatter` is an implementation choice; the invariant to restore is that no path emits a count or a document that contradicts the data it was handed. If the project would rather refuse the input at the write path instead, that is a separate change and does not remove this one: values already stored, and values arriving through `tick migrate`, still have to read back honestly. Extend the awkward fixture's carriers (internal/cli/round_trip_test.go) with one refused character so the round-trip assertion pins the behaviour chosen.

COMMENT_CORRECTIONS:
- internal/cli/format.go:240 — names the one formatter that reads the field, a cardinality claim ordinary additive change falsifies; the clause after it carries the part the code cannot
  OLD: 	// Message is the no-dependencies sentence. Only the pretty formatter renders it;
	// the machine formats answer an empty graph with the emptied document instead.
  NEW: 	// Message is the no-dependencies sentence; the machine formats answer an empty
	// graph with the emptied document instead.

SUMMARY: The field-selection module, the end-of-flags split, the merged `changed` table and the conformance inventory (with its coverage guard against `commandFlags`) compose cleanly, and cycle 2's full-graph edge list is now read off the stored relation with counts and edges agreeing on every branch I could reach, cycles and dangling blockers included. One seam does not hold: the new toon encoding helpers answer an encoder refusal by emitting a parseable document that denies the data — a dropped header block, a count-zero section, an empty task list — silently and with exit 0, which is the exact failure class this work was built to delete and one the parse-only conformance suite cannot detect.
