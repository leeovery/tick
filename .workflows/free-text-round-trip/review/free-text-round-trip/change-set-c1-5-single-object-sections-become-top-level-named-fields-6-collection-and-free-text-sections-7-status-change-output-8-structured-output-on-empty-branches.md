SECTION: 5. Single-Object Sections Become Top-Level Named Fields + 6. Collection and Free-Text Sections + 7. Status Change Output + 8. Structured Output on Empty Branches

SCOPE: Commit range eb88e700f3acf3cd0030a720bbcf9e5243bc30b7^..HEAD, filtered to the 57-file change-set. Read in final state: internal/cli/toon_formatter.go, format.go, transition.go, helpers.go, create.go, update.go, show.go, show_fields.go, note.go, stats.go, dep_tree.go, dep_tree_graph.go, json_formatter.go, pretty_formatter.go, internal/task/transition.go, internal/task/apply_cascades.go, README.md, CLAUDE.md; test files read for what they pin: status_change_test.go, detail_changes_test.go, toon_formatter_test.go, toon_decode_test.go, readme_samples_test.go. Read the specification's §5-§8 and all twelve corrigenda. Run: `gofmt -l ./internal ./cmd`, `go vet ./...`, `golangci-lint run ./...`, `go build -o <tmp>/tick ./cmd/tick/`. Measured by exercising the built binary against hand-written `.tick/tasks.jsonl` fixtures in the system temp directory (chain, diamond, 2-cycle, two disjoint cycles, dangling blocker, dangling-blocker-above-a-chain, re-parenting with two cascades, shared-ancestor triple cascade, values carrying commas/colons/newlines/leading dashes), and by decoding every produced TOON document with the project's own library (github.com/toon-format/toon-go v0.0.0-20251202084852, a throwaway `toon.DecodeString` harness built in the system temp directory). Byte-identity measured against binaries built from `git archive` of a1db9c48^ and 63b2e740^/63b2e740 into the system temp directory. Every temp tree, binary and fixture removed; `git status --porcelain` at the end matches its state at the start (M .workflows/free-text-round-trip/manifest.json, ?? .workflows/.inbox/bugs/, ?? .workflows/free-text-round-trip/review/). No tracked file touched, no git write.

MEASURED:
- "`tick dep tree` and `tick dep tree <id>` produce byte-identical output against a binary built before the change, under `--toon`, `--json` and `--pretty`, over a cycle, a diamond and a graph with a dangling blocker" [10-3] — built a1db9c48^ via `git archive a1db9c48^ | tar -x` + `go build`, then compared stdout for `dep tree` and `dep tree <id>` over all three fixtures × all three formats (9 full-graph + 24 focused comparisons, `cmp -s`) — holds: every one identical
- "No test file is changed; the diff is confined to `internal/cli/dep_tree_graph.go`" [10-3] — `git show --stat a1db9c48` → `internal/cli/dep_tree_graph.go | 20 +++---`, 1 file changed — holds
- "The fixture's summary reads `1 chain, longest: 2, 2 blocked` in all three formats" [8-2] — fixture tick-ghost1 → tick-e00001 → tick-e00002; `tick dep tree --pretty` → `1 chain, longest: 2, 2 blocked`; `tick dep tree` (toon) → `chains: 1` / `longest: 2` / `blocked: 2`; `tick dep tree --json` → `"chains": 1, "longest": 2, "blocked": 2` — holds
- "The single dangling blocker and the rooted chain are unchanged … and every participant still reaches every format" [8-2] — same fixture: toon `dep_tree[2]{from,to}` carries both stored edges, pretty and JSON both draw tick-ghost1 with tick-e00001 and tick-e00002 nested — holds
- "`formatFullDepTree` output decodes without error via `toon.DecodeString` for a populated graph" [3-2] — `tick dep tree` piped into a `toon.DecodeString` harness over three fixtures (diamond+chain, cycle+dangling blocker, empty graph) → DECODE_OK each, and the decoded `dep_tree` rows equal the stored `blocked_by` relation — holds
- "`formatFocusedDepTree` output decodes without error via `toon.DecodeString` on every branch" [3-3] — `tick dep tree <id>` decoded on all four branches: both directions (tick-100003), downstream only (tick-100001), upstream only (tick-100004), no dependencies (tick-300001) → DECODE_OK each — holds
- The eight decode criteria (stats both branches, full dep tree three branches, focused four branches, `show` two branches) [6-2] — all ten documents piped through `toon.DecodeString`: stats populated + empty, full dep tree populated/cycle+dangling/empty, focused ×4, `show` with every optional field and with none → DECODE_OK on all; additionally `changed` on the transition branch and the `changed[0]` create branch decode — holds
- "Full `tick show --json` output is byte-identical to today's, `changed` last when a mutation carries it" [4-10] — built 63b2e740 and 63b2e740^ via `git archive`, ran `show --json` on a task carrying parent, closed, tags, refs, notes, blockers and children → `cmp -s` identical; at HEAD `tick create --json` returns keys ending `…,"blocked_by","children","changed"` and `tick show --json` returns the same list without `changed` — holds
- "`go vet ./...`", "`gofmt -l ./internal ./cmd`", "`golangci-lint run ./...`" and "`go build ./...`" clean — named by roughly sixty criteria across [1-1]…[10-6] as part of a four-part toolchain clause — `gofmt -l ./internal ./cmd` → no output; `go vet ./...` → no output, exit 0; `golangci-lint run ./...` → `0 issues.`; `go build -o <tmp>/tick ./cmd/tick/` → succeeds — holds

NOT MEASURED:
- "`go test ./...`" / "`go test ./internal/cli`" — the remaining clause of the same ~sixty toolchain criteria across [1-1]…[10-6], and the test-run halves of [2-7], [3-6], [3-7], [3-8], [6-7], [6-8], [7-1], [7-2], [7-3] — the suite is reserved for the single run over the corrected tree; a single package's tests are permitted only to confirm a suspected defect, and I suspect none in this section
- "Pretty is byte-identical: … the README samples still match the built binary" [2-7] — pretty's cascade rendering is §4.1's subject, not §5-§8's; its byte-identity claim belongs to the section covering §1-§4
- "Output is byte-identical against a binary built before the change, in all three formats, filtered and full: `tick show <id>`, `--field notes.2`, `--field title,tags.2`, `--field children.1`" [10-1] — the field-selection subject is §9's; §6.3's half of it (a narrowed notes section keeps its whole-section index) was measured here and holds
- "Renaming a documented key or shifting a documented value in any of the three formatters fails this test — verified locally by making one such change, watching the failure, and reverting it" [3-8] — the verification is a deliberate mutation of a tracked file, which this pass may not make

FINDINGS:
- [in-scope] [contained] README.md:321 — the `dep tree` paragraph says the pretty tree "seeds a tree from each participant nothing already drawn reaches — a cycle's member, or a task blocked by an ID that no longer exists — with its downstream chain nested beneath it". Since `buildSeededTrees` holds a participant back while any of its blockers is undrawn (internal/cli/dep_tree_graph.go:264, `blockedByUnemitted`), a task blocked by a dangling ID is never the seed: the dangling ID itself is, rendered with status `missing` and an empty title (internal/cli/dep_tree_graph.go:250). Measured on a fixture where tick-ghost1 blocks tick-e00001 which blocks tick-e00002, `tick dep tree --pretty` prints `tick-ghost1   (missing)` at top level with both real tasks nested beneath — not the documented `tick-e00001  E1 (open)`. Replace the second example with the dangling blocker itself and state that it renders with status `missing` and no title (the README documents the `missing` token nowhere; its only status list, README.md:123, is the four real statuses) — FAILS: a reader predicting `tick dep tree` output for a project with a dangling blocker expects the blocked task as the top-level entry and gets a node the README never mentions, with a status token no part of the documentation explains; the README prose sits outside every fenced sample, so `TestREADMESamplesMatchRenderedOutput` cannot catch the drift

COVERAGE:
- the task's own fields are top-level named fields with no wrapping key (§5.2) — internal/cli/toon_formatter.go:228-261 → encodeToonFields at :266 — measured: `tick show <id>` → `id: …` / `title: …` / `status: …` / `priority: …` at column 0, no `task:` scope, no `task[1]{…}` header; `toon.DecodeString` returns a flat object with no `task` key
- optional head fields appear only when carried (§5.2) — internal/cli/toon_formatter.go:241-254 — measured: a task with no type/parent/closed emits none of the three lines; one carrying all three emits `type`, `parent`, `closed` in that order
- `blocked_by`, `children` and `notes` are always present with a count-zero header (§5.2, §8) — internal/cli/toon_formatter.go:78-96 → emptyToonSection at :327 — measured: `tick create "Standalone"` → `blocked_by[0]{id,title,status}:` / `children[0]{id,title,status}:` / `notes[0]{index,text,created}:`
- count-zero headers derive their columns from the row struct's toon tags rather than a hand-written string (§5.2) — internal/cli/toon_formatter.go:327-334 — read: `reflect.TypeFor[T]()` walks the fields and cuts each `toon:"…"` tag at its first comma, so a column added to toonNoteRow/toonRelatedRow/toonChangedRow/toonEdgeRow reaches the empty header without a second edit
- `tick stats` counts are top-level named fields beside the by_priority table (§5.2) — internal/cli/toon_formatter.go:110-130 — measured: `tick stats` → `total`/`open`/`in_progress`/`done`/`cancelled`/`ready`/`blocked` as named fields, blank line, `by_priority[5]{priority,count}:` with five rows; decodes to numbers, not strings
- the full dep-tree summary is top-level named fields beside the edge table (§5.2) — internal/cli/toon_formatter.go:182-193 — measured: `chains`/`longest`/`blocked` as named fields after `dep_tree[N]{from,to}:`
- the focused dep-tree document opens with the target's id/title/status exactly as a task detail does, on both branches (§5.2) — internal/cli/toon_formatter.go:195-207 — measured: populated target and no-dependency target both emit `id:` / `title:` / `status:` before the two edge sections
- tags and refs use the library's inline list form (§6.1) — internal/cli/toon_formatter.go:86-92 → encodeToonSection at :339 — measured: `tags[2]: alpha,beta`; a ref carrying a comma comes back as one element (`refs[2]: "https://example.com/a,b","plain:ref"` decodes to exactly those two strings), a URL colon is quoted by the encoder
- the hand-written list/description builders are gone (§6.1, §6.2) — internal/cli/toon_formatter.go — measured: `grep -rn 'encodeToonSingleObject|buildStringListSection|buildDescriptionSection|task\[1\]' internal cmd --include "*.go"` → no matches
- the description is one TOON-quoted value (§6.2) — internal/cli/toon_formatter.go:102-104 — measured: a description of `- leading dash\n  indented: colon\n"quoted"` renders as one quoted line and decodes back byte-identically, leading dash, interior indent, colon and embedded quotes intact
- the notes section carries a leading 1-based index column (§6.3) — internal/cli/toon_formatter.go:310-323 with positions from selectedItems at internal/cli/show_fields.go:76-98 — measured: full output → `notes[2]{index,text,created}:` rows indexed 1,2; `--field notes.2,notes.3` → `notes[2]{…}` rows indexed 2,3; `--field notes.3,id` → one row indexed 3; JSON carries the same `"index"` values
- note text carrying a newline, a comma or a leading dash survives the notes table (§6.3) — measured: `"multi\nline\nnote, with comma"` and `"- dash note"` both decode back exactly
- no `note edit` subcommand exists (§6.4) — internal/cli/note.go:28-35, internal/cli/help.go:148-154 — read: the switch has `add` and `remove` only, default returns "unknown note sub-command"; the help entry spells the usage as `tick note <add|remove>`
- every status change is one `changed` table with an `auto` column (§7.2) — internal/cli/toon_formatter.go:146-162 — measured: `tick start <child>` → `changed[2]{id,title,from,to,auto}:` with the requested row `…,false` first and the cascaded parent `…,true`; `tick done <parent>` → three rows, one per task that moved
- the auto column is the transition history's own flag, not a new vocabulary (§7.2) — internal/task/apply_cascades.go:58 and :104, internal/task/transition.go:8-12 — read: `applyWithCascades` sets `result.Auto = auto` and writes the same value into the primary's TransitionRecord; every cascaded record is written `Auto: true`, matching the `Auto: true` the table's cascade rows carry at internal/cli/format.go:218-226
- a task appears at most once and one that ends where it started carries no row (§7.2) — internal/cli/transition.go:109-159 — read: statusChangeSet keys on `task.NormalizeID`, merges to first-From/last-To, and `rows()` drops `c.From == c.To`; pinned by internal/cli/status_change_test.go:93-114 which fails if either behaviour breaks. Measured: a re-parenting that moves three tasks (new parent reopened, its ancestor reopened, old parent auto-completed) returns exactly three rows, one per task
- `done`/`start`/`cancel`/`reopen` return only the changed table (§7.3) — internal/cli/transition.go:58 → outputStatusChanges at internal/cli/helpers.go:109 — measured: `tick done <id>` stdout is the `changed` table alone, nothing before or after
- `create` and `update` return one document with `changed` as a section inside it (§7.3, §7.4) — internal/cli/create.go:277-278, internal/cli/update.go:397-398 → outputMutationResult at internal/cli/helpers.go:17-32 — measured: `tick create --parent <done task>` returns the full record with `changed[1]{…}` as its last-but-one section and no loose lines after the document; the whole stream decodes as one TOON object
- the changed section is always there on create and update (§7.4) — internal/cli/create.go:277 and internal/cli/update.go:397 both pass a non-nil `&StatusChanges{}` — measured: `tick create "Standalone"` (no parent) → `changed[0]{id,title,from,to,auto}:`; `tick update <id> --priority 1` (no status move) → the same count-zero header
- `show`, `note add` and `note remove` carry no changed section (§7.3) — internal/cli/show.go:83-84 leaves Changes nil, internal/cli/note.go:87 and :145 pass nil — measured: all three commands' toon output carries no `changed` line and their JSON key list ends at `children`
- a structural change is what produces the cascade, and both of §7.5's cases fire (§7.5) — internal/cli/helpers.go:118-137 (Rule 6/5 on the new parent) and internal/cli/update.go:364-370 (Rule 3 on the original parent) — measured: `tick create --parent <done>` → one row reopening the parent; `tick update <id> --parent <other done>` → two rows, the new parent reopened and the old parent auto-completed, both `auto` true, matching §7.2's second example exactly
- unchanged terminal children are not reinstated (§7.6) — measured: a parent with one already-done child and one open child, closed with `tick done` → two rows (the parent and the open child); the already-done child gets none
- both empty dep-tree branches return the emptied document in toon and JSON (§8) — internal/cli/toon_formatter.go:182-207, internal/cli/json_formatter.go:380-401 — measured: empty graph → `dep_tree[0]{from,to}:` with `chains: 0`/`longest: 0`/`blocked: 0` and JSON `{"mode":"full","trees":[],"chains":0,…}`; named task with no dependencies → `id`/`title`/`status` plus `blocked_by[0]{from,to}:` and `blocks[0]{from,to}:`; neither formatter reads `DepTreeResult.Message` (`grep -n '\.Message' internal/cli/toon_formatter.go internal/cli/json_formatter.go` → no matches)
- pretty keeps the sentence, and only when the graph holds no participant at all (§8, §4.1's third corrigendum) — internal/cli/dep_tree_graph.go:166-169 sets Message only when `len(trees) == 0`, internal/cli/pretty_formatter.go:375-378 — measured: a project with tasks but no dependencies → `No dependencies found.`; a project whose only participants are a dangling blocker and the task it blocks → the tree is drawn and the sentence does not fire
- the full-graph edge list is the stored relation, one row per recorded dependency in record order, repeating none (§8) — internal/cli/dep_tree_graph.go:182-203 — measured: the four-command diamond that used to return four rows returns `dep_tree[3]{from,to}` beside `chains: 1`; a dangling blocker above a chain returns two rows for two stored dependencies
- the counts and the edge list never disagree, including when no participant is a root (§8) — internal/cli/dep_tree_graph.go:145-158 and :288-331 — measured: a 2-cycle → `dep_tree[2]` with `chains: 1`, `blocked: 2`; two disjoint 2-cycles → `dep_tree[4]` with `chains: 2`, `blocked: 4`; cycle plus dangling blocker → `dep_tree[3]` with `chains: 2`, `blocked: 3`
- the focused edge sections are the relation scoped to the neighbourhood (§8's last corrigendum) — internal/cli/dep_tree_graph.go:359-360 → collectScopedStoredEdges at :189 — measured: the diamond's sink returns `blocked_by[3]` for the three stored dependencies above it with the shared branch unrepeated; a cycle's member returns every one of the cycle's stored edges including the walk's suppressed re-entry
- a focused blocker that names no task contributes no row, matching what `tick show` lists (§8's last corrigendum) — measured: the ghost-blocked task returns `blocked_by[0]{from,to}:` and `tick show --json` returns `"blocked_by": []`
- the node-shaped renderings seed each undrawn participant and resume after the cycle fallback (§8) — internal/cli/dep_tree_graph.go:246-285 — read: the outer loop re-runs after each fallback seed and `seed` marks the whole drawn subtree emitted, so the unemitted-seed set strictly shrinks and the loop terminates. Measured: two disjoint 2-cycles are both drawn in pretty and JSON
- a participant that names no task renders with status `missing` and an empty title (§8) — internal/cli/dep_tree_graph.go:238 and :250 — measured: pretty prints `tick-ghost1   (missing)`, JSON returns `{"id":"tick-ghost1","title":"","status":"missing"}`, and the toon edge list carries the ID only
- the toon documents this section defines are decodable by the project's own library on every branch — measured: thirteen documents (full dep tree ×3, focused ×4, stats ×2, show ×2, changed ×2) piped through a `toon.DecodeString` harness → DECODE_OK on all, with every value round-tripping to the stored string
- the delivered tests pin these properties rather than naming them — internal/cli/toon_formatter_test.go:724-766 (head keys and order, `task` key absent), :663-723 (note index on the populated and the empty section), :575-632 (inline tags/refs, comma-bearing ref kept whole), :244-318 (description as one quoted value, seven round-trip cases), :1040-1130 (emptied dep-tree documents, no prose), internal/cli/detail_changes_test.go:57-232 (changed present/count-zero/absent, placement, and the three commands that carry none), internal/cli/status_change_test.go:93-182 (collapse, drop-on-no-op, first-seen order, non-nil empty), internal/cli/readme_samples_test.go:425-452 (every prompted README fence rendered by the real formatter and compared byte-for-byte, one declared exemption which is a flag-error fence) — read: each asserts the decoded value or the exact header, so each goes red if the property it names breaks
