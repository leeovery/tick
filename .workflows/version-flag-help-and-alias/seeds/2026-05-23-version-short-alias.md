# Add -V short alias for --version

No short alias `-V` was added (per spec exclusion). Trivial single-line addition to `applyGlobalFlag` if requested later.

Source: review of add-version-flag/add-version-flag
